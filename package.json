{
  "name": "guardos-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix guardos && npm run build --prefix guardos",
    "dev": "npm run dev --prefix guardos"
  }
}
